services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: quotes
      POSTGRES_USER: quotes
      POSTGRES_PASSWORD: quotes
    ports:
      - "5432:5432"
    volumes:
      - quotes_pg_data:/var/lib/postgresql/data

volumes:
  quotes_pg_data: